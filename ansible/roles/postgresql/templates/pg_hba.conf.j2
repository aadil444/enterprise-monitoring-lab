# Local connections
local   all             all                                     peer

# IPv4 local connections
host    all             all             127.0.0.1/32            scram-sha-256

{% for host in postgresql_allowed_hosts %}
host    zabbix          zabbix          {{ host }}/32           scram-sha-256
{% endfor %}