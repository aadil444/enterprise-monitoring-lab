[defaults]

inventory = inventory/hosts.yml

roles_path = roles

remote_user = cadmin

host_key_checking = False

retry_files_enabled = False

forks = 10

timeout = 30

# Built-in default callback with YAML output
stdout_callback = default
result_format = yaml

interpreter_python = auto_silent
